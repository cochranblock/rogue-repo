# rogue-repo

- Sovereign software repo + ISO 8583 payment engine. Nested: rogue-repo/ (PWA), rogue-runner/ at repo root.
- Build: cargo build -p rogue-repo or cargo build -p rogue-runner (from ROGUE_REPO_ROOT)
- Paths relative to repo root
